import styled from "styled-components";

const css = styled.div`
  // display: flex;
  // flex-wrap: wrap;
  // justify-content: center;

  display: flex;
  flex-direction: column;
`;

export default css;
